{{page-title this.model.title}}

<div class="p-6 grid md:grid-cols-2 gap-8 max-w-6xl mx-auto">
  <Product::ImageAndTitle @product={{this.model}} />
  <div class="flex flex-col gap-6">
    <Product::Details @product={{this.model}} />
    <Product::Actions @product={{this.model}} />
  </div>
</div>
